/* Code Prompt
------------------------------------------------------------
Create two arrays to hold 10 integer numbers each

Write a function to fill an array with 10 random numbers - the function signature should be: void fillArray(int arr[], int size, int min, int max). This function should call your randBetween function.

Write a printArray function that prints the arrays neatly.

in main, call printArray to print the first array.

From main, call the fillArray function to fill your first array with random numbers between -100 and 100.

Write a function (doubleArray). doubleArray  takes two arrays as input.  The first array has values, the second array is the same size as the first array, but is empty.  The function takes each value in the first array, doubles it, and enters it into the corresponding value in the second array. The value in the first array is not modified.

from main, call the doubleArray function to fill the array.

in main, call printArray to print the second array. 

-------------------------------------------------------------
*/



// Declare global variables

// Create fill array function
// {
    // Random number generation algorithm
// }

// Create print array function (create array, size, format, and cout argument)
// {
       //  for loop that prints all elements in the array 
// }

// Create double array function ( create an array and size arguemnt)
// {
        // range-based for loop that copies that elements of the array  
        // and pastes it to the second array in the double array function argument
// }

// Create main function
// {
     // Initialize the min, max and format variable in main

     // call fillArray function and arguments

     // call printArray function and arguments

     // call doubleArray function and arguments

     // call printArray function and arguments again

     // terminate function
// }
